<?php
/**
 * @file
 * OpenID Connect Vihreät module.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function openid_connect_vihreat_ctools_plugin_directory($owner, $plugin_type) {
  if ($owner == 'openid_connect') {
    return 'plugins/' . $plugin_type;
  }
}
